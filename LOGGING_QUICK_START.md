# Быстрый старт: Логирование операций

## 🚀 Для запуска на сервере

### 1. Создайте таблицу в БД (один раз)

```sql
-- Выполните в MS SQL Server:
sqlcmd -S ваш_сервер -U ваш_юзер -P ваш_пароль -i create_operation_logs_table.sql
```

### 2. Пересоберите проект

```bash
# Docker (продакшн)
npm run docker:stop
npm run docker:prod

# Docker (разработка)
npm run docker:dev
```

### 3. Проверьте работу

```bash
# Тестовый запрос
curl http://localhost:3006/api/storage/info

# Проверьте логи
curl http://localhost:3006/api/logs?limit=5
```

## 📊 Основные эндпоинты

```bash
# Получить последние 100 логов
GET /api/logs

# Получить логи с фильтрами
GET /api/logs?executor=user_123&operation_result=error

# Получить статистику
GET /api/logs/statistics
```

## 🔍 Быстрые SQL запросы

```sql
-- Последние 10 логов
SELECT TOP 10 * FROM x_API_Operation_Logs ORDER BY created_at DESC;

-- Количество ошибок за последний час
SELECT COUNT(*) FROM x_API_Operation_Logs 
WHERE created_at >= DATEADD(hour, -1, GETDATE()) AND operation_result = 'error';

-- Очистка логов старше 90 дней
DELETE FROM x_API_Operation_Logs WHERE created_at < DATEADD(day, -90, GETDATE());
```

## 📚 Документация

- **Полная документация:** [OPERATION_LOGGING.md](OPERATION_LOGGING.md)
- **Инструкция развертывания:** [DEPLOY_LOGGING.md](DEPLOY_LOGGING.md)
- **Swagger UI:** http://localhost:3006/api-docs

## ✅ Что логируется автоматически

- ✅ Все HTTP запросы (GET, POST, PUT, DELETE)
- ✅ Параметры запросов
- ✅ IP адреса и User Agent
- ✅ Время выполнения
- ✅ Коды ответов и ошибки

## 🔒 Безопасность

- Пароли и токены автоматически фильтруются
- Чувствительные данные заменяются на `***FILTERED***`

## 📌 Примечание

Система работает автоматически после развертывания. Никаких дополнительных настроек не требуется.
