# Остановка и удаление существующих контейнеров
docker-compose down

# Сборка и запуск контейнеров в производственном режиме
docker-compose up --build -d

# Вывод логов
Write-Host "Контейнеры запущены в фоновом режиме. Для просмотра логов используйте: docker-compose logs -f" 